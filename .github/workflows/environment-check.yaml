name: check-environments

on:
  push:
    branches:
      - main
      - 'feature/*'

jobs:
  print-vars:
    environment: production
    runs-on: ubuntu-latest
    steps:
      - name: Print vars
        run: echo "ENV"